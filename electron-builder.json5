// @see https://www.electron.build/configuration/configuration
{
  $schema: 'https://raw.githubusercontent.com/electron-userland/electron-builder/master/packages/app-builder-lib/scheme.json',
  appId: 'aliceaiapp',
  asar: true,
  productName: 'Alice AI App',
  directories: {
    output: 'release/${version}',
  },
  publish: [
    {
      provider: 'github',
      owner: 'xiezixka',
      repo: 'Alice',
      releaseType: 'release',
    },
  ],
  files: ['dist', 'dist-electron'],
  extraResources: [
    {
      from: './app-config.json',
      to: 'app-config.json',
    },
    {
      from: 'resources/backend',
      to: 'backend',
      filter: ['**/*'],
    },
    {
      from: 'backend/lib',
      to: 'backend/lib',
      filter: ['**/*'],
    },
    {
      from: 'backend/models',
      to: 'backend/models',
      filter: ['**/*'],
    },
  ],
  mac: {
    // DMG is the user-facing installer; ZIP is required by electron-updater
    // for macOS differential/full updates.
    target: ['dmg', 'zip'],
    icon: 'public/app_logo.png',
    entitlements: 'build/entitlements.mac.plist',
    entitlementsInherit: 'build/entitlements.mac.plist',
    extendInfo: {
      NSMicrophoneUsageDescription: 'Alice 需要使用麦克风进行语音活动检测和语音识别。',
    },
    artifactName: 'Alice-AI-App-Mac-${version}-Installer.${ext}',
  },
  win: {
    target: [
      {
        target: 'nsis',
        arch: ['x64'],
      },
    ],
    artifactName: 'Alice-AI-App-Windows-${version}-Setup.${ext}',
  },
  nsis: {
    oneClick: true,
    perMachine: false,
    allowToChangeInstallationDirectory: false,
    deleteAppDataOnUninstall: false,
  },
  compression: 'store',
  linux: {
    target: ['AppImage'],
    category: 'Utility',
    icon: 'public/app_logo.png',
    artifactName: 'Alice-AI-App-Linux-${version}.${ext}',
  },
}
